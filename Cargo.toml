[workspace]
resolver = "2"
members = ["app", "server", "types"]

[profile.release]
strip = true
opt-level = "z"
lto = true

# Enable a small amount of optimization in the dev profile.
[profile.dev]
opt-level = 1

# Enable a large amount of optimization in the dev profile for dependencies.
[profile.dev.package."*"]
opt-level = 3
